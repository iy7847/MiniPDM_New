# MiniPDM v2.0 프로젝트 인수인계서 (HANDOFF.md)

이 문서는 다음 세션의 AI 에이전트가 즉시 프로젝트의 컨텍스트를 파악하고 작업을 이어나갈 수 있도록 작성된 인수인계서입니다. 작업 시작 시 이 문서를 최우선으로 참고하세요.

---

## 🎯 1. 현재 도달한 Phase 및 목표

- **현재 위치**: Phase 1~3의 기술 부채(거대 컴포넌트) 청산 완료 ➡️ **Phase 4 (수주 / 생산 / 출하) Mini MES 기획 완료 및 진입**
- **다음 목표**: `docs/MINI_MES_PLAN.md`에 명시된 기획안에 따라 **"0단계. 수주 번호 발급 및 수주 확정 로직(RPC)"**부터 실제 백엔드 개발 착수.

---

## ✅ 2. 직전 세션 작업 완료 내역

직전 세션에서는 수주 관리 모듈(`src/features/orders`)의 핵심인 **부분 생산 이관(Partial Handoff)** UX와 백엔드 연동을 완벽하게 구축했습니다.

1. **부분 이관 (Partial Handoff) 완벽 구현**
   - 1-Depth BOM 내의 개별 품목들을 선택적으로 먼저 현장으로 넘길 수 있는 부분 이관(`PRODUCTION_READY`) 기능을 완성했습니다.
   - 부분 이관 시 전체 수주 상태는 `[부분 이관됨]`(경고 뱃지)으로 스마트하게 표기되며, 대기 중인 다른 품목들은 계속 수정 가능하도록 분리했습니다.
2. **동적 상태 토글 (Clickable Status Badge)**
   - 테이블의 상태 뱃지를 클릭 가능한 버튼으로 구현하여, 마우스 클릭 한 번으로 해당 품목을 즉시 생산 이관하거나 취소(롤백)할 수 있는 직관적인 UI를 적용했습니다.
3. **스마트 다중 처리 툴바 (OrderBatchToolbar)**
   - 여러 품목을 체크박스로 선택 시 나타나는 툴바에서 선택된 품목의 상태(수주 대기 vs 생산 이관)를 감지해, `[수주 확정]` 또는 `[이관 취소]` 버튼을 동적으로 분기하여 보여줍니다.
   - 부분 이관된 품목은 자동으로 입력 필드 락(Lock)이 걸려 데이터 무결성을 보호합니다.
4. **라벨 출력 UX 개편**
   - 헤더에 있던 라벨 출력 버튼을 제거하고, 테이블에서 체크박스를 선택했을 때 나타나는 '컨텍스트 툴바'로 이동시켜 직관성을 극대화했습니다.

(이전 리팩토링 및 기능 내역)
- 수주 목록 필터링, 기간 조회 & Sticky Filters (URL/sessionStorage 동기화)
- 견적서 없는 신규 수주 직접 등록 (Direct Order)
- `EstimateItemModal.tsx`, `TemplateBuilderModal.tsx` 등 거대 UI 컴포넌트 5종 하위 분리
- `tsc --noEmit` 타입 체크 100% 통과 유지

---

## 🚀 3. 다음 세션(Next Session)에 해야 할 핵심 작업

1. **최우선 읽기 파일**: 작업 시작 전 반드시 `docs/MINI_MES_PLAN.md` 파일을 먼저 정독하여, 합의된 1-Depth BOM 구조 및 Ad-hoc 공정 관리 등 Mini MES의 전체 설계 철학을 100% 파악하세요.
2. **Phase 4 (Mini MES) - DB 스키마 설계 및 백엔드 개발 착수**: 
   - 기획안(`docs/MINI_MES_PLAN.md`)의 내용에 따라, 본격적으로 생산 관리 대시보드를 위한 DB 마이그레이션(예: `supply_type`, `use_stock` 컬럼 추가 및 외주/소재 발주 N:M 테이블 설계)을 기획하고 확정받은 뒤 진행하세요.
   - 부분 이관(`PRODUCTION_READY`) 처리된 품목들을 묶어서 소재/외주 발주를 내는 '생산 관리 리스트' 화면 UI 구축을 시작하세요.
3. **신규 기능 개발 시 원칙 준수 (중요)**
   - **UI 300줄 / Page 500줄 이하 원칙**을 반드시 지키세요.
   - 데이터 목록 화면 개발 시 `AGENTS.md`의 **"URL 기반 상태 동기화 및 상태 유지(Sticky Filters)"** 규칙을 엄격하게 적용하세요.

---

## 📝 4. 주요 참고 사항
- 개발 중 막히거나 구현 계획이 필요할 때는 사용자와 적극적으로 상의(구현 계획서 제시)한 뒤 진행하세요.
- GitHub 관련 명령어는 절대 임의로 실행하지 마세요. (사용자 명시적 허락 필수)
