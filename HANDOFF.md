# 🤖 MiniPDM v2.0 프로젝트 인수인계서 (HANDOFF.md)

> 이 문서는 이전 세션의 작업 내역을 다음 세션에 이어가기 위해 정리한 문서입니다. 다음 AI 에이전트의 작업 시작 시 이 문서를 반드시 가장 먼저 숙지하세요.

## 1. 프로젝트 현재 상태 (완료된 작업)
현재 **MiniPDM v2.0의 프론트엔드 프리미엄 UI 구축(Phase 1)** 및 **기준 정보/단가 관리 기능 고도화(Phase 2)**에 이어, **견적 관리 기능(Phase 3)의 세부 모달 사용성 개선 및 버그 수정**을 진행했습니다.

- **과거 견적 품목 검색(EstimateItemSearch) 기능 및 UI 개선**
  - **장바구니 모드 지원**: 좌측 바구니(현재 작성 중인 견적/새 견적) 패널과 우측 검색 패널의 2단 구조로 분리하여 과거 견적서 품목을 쉽게 조회하고 장바구니에 담을 수 있게 구현했습니다.
  - **상세 필터 추가**: 단순 텍스트 검색을 넘어 `비고 내용 검색`, `규격 오차범위 검색`, 그리고 견적서의 **진행 상태(작성중, 제출완료, 수주확정)** 탭을 추가하여 고도화된 검색을 지원합니다.
  - **목록 상태 동기화 및 버그 픽스**: 상태 필터 선택 시 빈 값이어도 즉시 검색되도록 Early Exit 로직을 개선하고, 결과 목록에 품목이 속한 '견적 상태'를 배지(Badge) 형태로 함께 표기하도록 변경했습니다.
  - **프리미엄 UI 적용**: 투박한 이모지를 `lucide-react` 아이콘으로 변경하고, 검색바에 `backdrop-blur` 및 모던한 `gap` 레이아웃을 적용해 세련된 프리미엄 디자인 원칙을 준수했습니다.

- **견적 품목 수정 창(모달) UI 및 UX 개선**
  - **아코디언 구조 적용**: 모달 좌측의 입력 폼(기본 규격 및 형상, 공정 비용 계산, 수량 및 최종 단가) 영역을 확장/축소(Accordion)할 수 있도록 구현하여 좁은 화면에서도 효율적인 작업이 가능하게 했습니다.
  - **파일 첨부 UI 간소화**: 기본 파일 첨부 `<input>`을 제거하고, 우측 상단 파일 요약(뱃지) 텍스트를 `[📄 2D (1)]`, `[🧊 3D (1)]` 형태로 짧고 간결하게 통일했습니다.
  - **파일 미리보기 탭 아이콘 동기화**: 우측 PDF/이미지 뷰어 탭의 파일명 좌측에 확장자를 분석하여 해당하는 2D/3D 아이콘을 표시하도록 구현했습니다.
  - **뷰어 환경 최적화**: 미리보기 iframe에 노출되던 브라우저 기본 PDF 툴바(다운로드, 인쇄 등)를 비활성화(`toolbar=0`)하여 도면 자체에만 집중할 수 있게 다듬었습니다.

- **드래그 앤 드롭 및 파일 연동 관련 핵심 버그 수정**
  - **이벤트 버블링 차단**: 품목 수정 모달에서 드래그 앤 드롭으로 파일을 첨부할 때, 이벤트가 부모(견적 상세 페이지)로 전파되어 '스마트 도면 매칭' 창이 오작동하는 버그를 `e.stopPropagation()`으로 원천 차단했습니다.
  - **임시 파일(tempFiles) 유지**: 견적 상세 목록에서 글로벌 드롭존을 통해 첨부된 파일이, 품목 수정 모달을 여는 순간 초기화되어 사라지던 문제를 수정하여 정상적으로 연동되도록 조치했습니다.

## 2. 아키텍처 및 핵심 패턴
- **스택**: React 18 + TypeScript + Vite + Tailwind CSS v3
- **상태 관리**: `Zustand` (도메인별 분리)
- **컴포넌트 구조**: 
  - `src/design-system/`: 범용 UI 컴포넌트 (공용 컴포넌트 최우선 규칙 적용)
  - `src/features/`: 도메인 비즈니스 로직 및 페이지
  - `src/shared/`: 타입, 훅, 유틸리티

## 3. 다음 세션(Next Session) 진행 목표 및 방향

다음 세션에서는 **[Phase 3] 견적 관리 및 수주 연동 기능**과 **스마트 도면 매칭**의 완성도 제고를 이어가야 합니다.

1. **스마트 도면 매칭 및 파일 파서(Parser) 로직 고도화**
   - 현재 글로벌 드롭존을 통해 띄워지는 `FilenameParserModal`의 UI 정렬 및 파싱 로직 세밀화 작업 검토.
2. **견적 → 수주 전환 플로우 테스트 및 개선**
   - 확정된 견적을 수주로 넘기는 과정의 데이터 정합성 검증 및 프리미엄 UI 적용 여부 확인.
3. **단가 계산 로직 점검**
   - `Scales.md` 공식을 기반으로 모달 내에서 입력되는 치수, 중량에 따른 단가 실시간 자동 계산 로직 최종 점검 및 오류 수정.

## 4. 특이사항 (주의점)
- `DIRECTIVE.md`와 `AGENTS.md`의 디자인 원칙(무조건 다크 테마, 공용 컴포넌트 필수 사용)을 엄격하게 지키세요. 
- 글로벌 사용자 규칙에 따라 **깃허브 관련 명령어(git push 등)는 사용자의 명시적인 승인 전에는 절대 실행하지 마세요.**
- 데이터 리스트 화면(테이블)은 한눈에 많은 정보를 파악할 수 있도록 전체 너비(`w-full`)를 사용하세요.
- URL Parameter 기반의 검색/필터 유지(Sticky Filters) 훅(`use[Feature]List`)을 다른 리스트 페이지 작업 시에도 동일하게 적용하세요.
