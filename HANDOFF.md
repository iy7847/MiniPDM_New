# 🤖 MiniPDM v2.0 프로젝트 인수인계서 (HANDOFF.md)

> 이 문서는 이전 세션의 작업 내역과 다음 세션에서 이어갈 목표를 정리한 문서입니다. 다음 AI 에이전트는 작업 시작 전에 이 문서를 반드시 숙지하세요.

## 1. 프로젝트 현재 상태 (완료된 작업)
현재 **MiniPDM v2.0의 프론트엔드 프리미엄 UI 구축 및 실제 DB(Supabase) 연동 진행, 권한(RBAC)/인적사항 관리 기능 구현**이 진행 중입니다.

- **디자인 시스템 및 Tailwind 맵핑 완전 복구**: `DIRECTIVE.md`에 명시된 다크 테마(bg-base, bg-surface, bg-elevated)와 브랜드 컬러(brand-500)가 `tailwind.config.js`와 정확히 맵핑되도록 전역 소스코드 스크립트 치환 작업을 완료했습니다.
- **환경 설정(Settings) 레이아웃 모던화 및 기능 완성**: 5개의 독립된 탭(기본 정보, 단가 및 마진, 할인율 정책, 견적 양식, 엑셀 프리셋)에 추가로 **사용자 관리, 그룹 관리** 탭을 신설했습니다.
- **확장형 권한 시스템(RBAC) 및 인사 정보 구축 완료**: 
  - `user_groups` 테이블을 생성하여 부서/그룹별 권한 관리를 구현했습니다.
  - 직원 클릭 시 우측에서 인적사항(직함, 연락처, 입사일 등)을 등록/수정할 수 있는 HR 폼을 연동했습니다 (`PhoneInput` 적용 완).
  - RLS(Row Level Security) 정책 버그 수정 (super_admin 등급의 권한 문제 해결).
- **커스텀 견적서 양식 빌더 구현 및 UI 버그 수정 (Template Builder)**:
  - 사용자가 견적서를 커스텀 디자인할 수 있는 `TemplateBuilderModal`을 개발했습니다.
  - 모달 상단이 잘려나가는 버그를 React `createPortal`을 적용하여 브라우저 최상단으로 띄워 완벽하게 해결했습니다.

## 2. 아키텍처 및 핵심 패턴
- **스택**: React 18 + TypeScript + Vite + Tailwind CSS v3
- **상태 관리**: `Zustand` (도메인별 분리: `useMesStore`, `useSettingsStore`, `useAppStore` 등)
- **컴포넌트 구조**: 
  - `src/design-system/`: 범용 UI 컴포넌트 (공용 컴포넌트 최우선 규칙 적용)
  - `src/features/`: 도메인 비즈니스 로직 및 페이지
  - `src/shared/`: 타입, 스토어, 유틸리티

## 3. 다음 세션(Next Session) 진행 목표 및 방향

다음 세션에서는 템플릿 빌더와 환경 설정이 완성되었으므로, **견적 관리 기능 고도화**와 **PDF 출력 엔진 연동**, 그리고 **기초 데이터 관리 화면**을 구축해야 합니다.

1. **거래처(Clients) 및 자재(Materials) 기초 데이터 화면 개발 (최우선 순위 1)**
   - 기초 정보 관리 화면 구현 및 Supabase DB 연동.
   - 폼 입력 시 새로 만든 `BaseInput`, `PhoneInput`, `BizNoInput` 등 공용 포맷 컴포넌트 필수 적용.
2. **커스텀 견적서 양식 기반 PDF 렌더링**
   - 구축된 견적 양식(JSON Layout 데이터)을 읽어들여 실제 A4 사이즈의 PDF 문서로 렌더링하고 인쇄/저장할 수 있는 기능(Print/Export) 구현.
   - html2canvas / jspdf 라이브러리 연동 혹은 브라우저 네이티브 인쇄 유도 지원.
3. **견적 관리 페이지 전면 리디자인**
   - 현재 구형 UI 상태인 `EstimatesPage.tsx`를 환경 설정 페이지와 동일한 수준의 프리미엄 레이아웃으로 개편.
   - 새로 만든 커스텀 양식을 선택하여 견적서를 발행할 수 있도록 워크플로우 개선.
4. **견적 / 단가 산출 로직 연동**
   - `Settings`에서 저장한 단가, 마진, 할인율 정책 전역 상태 값을 불러와서 견적 산출 로직(`Scales.md` 참조)에 100% 반영.

## 4. 특이사항 (주의점)
- `AGENTS.md`에 컴포넌트 길이 제한이 현실적으로 완화되었습니다. (UI 컴포넌트 300줄, 페이지 컴포넌트 500줄 이하) 무리한 쪼개기보다 완성도에 집중하세요.
- `DIRECTIVE.md`와 `AGENTS.md`의 디자인 원칙(무조건 다크 테마, 공용 컴포넌트 필수 사용)을 엄격하게 지키세요. 인라인 스타일을 지양하고 반드시 `design-system/`의 공용 컴포넌트를 조립하여 만드세요.
