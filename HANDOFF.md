# 🤖 MiniPDM v2.0 프로젝트 인수인계서 (HANDOFF.md)

> 이 문서는 이전 세션의 작업 내역을 다음 세션에 이어가기 위해 정리한 문서입니다. 다음 AI 에이전트의 작업 시작 시 이 문서를 반드시 가장 먼저 숙지하세요.

## 1. 프로젝트 현재 상태 (완료된 작업)
현재 **MiniPDM v2.0의 프론트엔드 프리미엄 UI 구축(Phase 1)** 및 **기준 정보/단가 관리 기능 고도화(Phase 2)** 작업이 성공적으로 완료되었습니다.

- **프리미엄 UI 기반 마련 (Phase 1)**
  - 다크 테마(Tailwind CSS v3 + CSS Variables) 기반의 디자인 시스템과 공통 컴포넌트를 구축했습니다.
  - 사이드바 메뉴 명칭 최적화(단가 관리, 생산 현황 등), 메뉴 그룹화 및 확장/축소 상태 저장(localStorage) 기능을 구현 완료했습니다.
  
- **단가 관리 핵심 로직 완성 (Phase 2)**
  - 자재, 후처리, 열처리 탭으로 구성된 통합 단가 관리 페이지(`MaterialsPage`)를 구축했습니다.
  - 품목 1개당 여러 거래처의 단가, MOQ, 비고를 멀티로 입력할 수 있는 다이내믹 폼(`FormRepeater`) 기능을 도입했습니다.
  - DB 스키마 간의 컬럼명 차이(`unit_price` vs `price_per_kg`)를 파악하여 폼 데이터 저장 시 완벽하게 분리 매핑하도록 개선했습니다.
  - 읽기 전용 사용자와 관리자 권한을 분리하고, 거래처 뱃지 클릭만으로 쉽게 내용을 훑어볼 수 있는 읽기 전용 모달(팝업) 뷰를 적용하여 사용성을 극대화했습니다.

## 2. 아키텍처 및 핵심 패턴
- **스택**: React 18 + TypeScript + Vite + Tailwind CSS v3
- **상태 관리**: `Zustand` (도메인별 분리)
- **컴포넌트 구조**: 
  - `src/design-system/`: 범용 UI 컴포넌트 (공용 컴포넌트 최우선 규칙 적용)
  - `src/features/`: 도메인 비즈니스 로직 및 페이지
  - `src/shared/`: 타입, 훅, 유틸리티

## 3. 다음 세션(Next Session) 진행 목표 및 방향

다음 세션에서는 **[Phase 3] 견적 관리 기능 고도화**에 착수해야 합니다. 현재 기획안(`implementation_plan.md`)이 작성되어 사용자의 승인을 기다리는 중입니다.

1. **견적 관리 메인 화면 스플릿 뷰(2-Panel) 구축**
   - 견적 목록과 상세 품목을 한 화면에서 좌우로 분할해서 보는 엑셀 스타일의 화면 레이아웃 적용.
   - `features/estimates/EstimatesPage.tsx` 및 `EstimateList`, `EstimateDetailPanel` 개발.
2. **단가 계산 로직 고도화**
   - `Scales.md` 공식을 기반으로 소재비, 가공비, 열처리비, 후처리비를 자동 계산하는 `useEstimateCalculations.ts` 훅 개발.
   - Phase 2에서 구축한 최신 자재/후처리 단가 DB 실시간 연동.
3. **사용자 인터랙션(UX) 구현 결정 (승인 대기 중)**
   - 견적 품목의 인라인 편집(Inline Edit) 기능 적용 여부.
   - 기존 스마트 PDF 파서의 이식 시점 결정.

## 4. 특이사항 (주의점)
- `DIRECTIVE.md`와 `AGENTS.md`의 디자인 원칙(무조건 다크 테마, 공용 컴포넌트 필수 사용)을 엄격하게 지키세요. 
- 인라인 스타일 작성을 지양하고 반드시 `design-system/`의 공용 컴포넌트를 조립하여 구성하세요.
- 데이터 리스트 화면(테이블)은 한눈에 많은 정보를 파악할 수 있도록 전체 너비(`w-full`)를 사용하세요.
