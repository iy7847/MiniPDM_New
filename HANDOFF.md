# 🚀 MiniPDM v2.0 프로젝트 인수인계서 (HANDOFF.md)

> 이 문서는 이전 세션의 작업 내역과 다음 세션에서 이어가야 할 목표를 정리한 문서입니다. 다음 AI 에이전트는 작업 시작 전 이 문서를 반드시 숙지하세요.

## 1. 프로젝트 현재 상태 (완료된 작업)
현재 **MiniPDM v2.0의 프론트엔드 프로토타입 완료 후, 실제 DB(Supabase) 연동 및 글로벌 레이아웃/디자인 시스템 고도화**가 진행 중입니다.

- **디자인 시스템 및 Tailwind 맵핑 완전 복구**: `DIRECTIVE.md`에 명시된 다크 테마(bg-base, bg-surface, bg-elevated)와 브랜드 컬러(brand-500)가 `tailwind.config.js`와 정확히 맵핑되도록 전역 소스코드 스크립트 치환 작업을 완료했습니다.
- **환경 설정(Settings) 레이아웃 모던화 및 기능 완성**: 기존의 밋밋한 폼 레이아웃을 걷어내고, 5개의 독립된 탭(기본 정보, 단가 및 마진, 할인율 정책, 견적 양식, 엑셀 프리셋)으로 화면 분할 및 모듈화. 프리미엄 SaaS 대시보드 레이아웃(Card + CSS Grid)으로 전면 개편했습니다.
- **DB 연동 진행 및 버그 픽스 (Supabase)**: 로그인 유저 프로필 조회(`company_id`) 누락 버그를 수정하여 `companies` 및 `excel_export_presets` 테이블과 완벽히 양방향 데이터 동기화가 이루어집니다.
- **주요 인터랙션 UI 이식 완료**: 
  - `chart.js`와 플러그인을 설치해, 차트의 점을 직접 드래그하여 수량별 할인율 곡선을 시각적으로 조절하는 핵심 기능을 이식했습니다.
  - 엑셀 프리셋 Drag & Drop 기능을 다크 테마 기반으로 100% 포팅 및 DB와 연동시켰습니다.

## 2. 아키텍처 및 핵심 패턴
- **스택**: React 18 + TypeScript + Vite + Tailwind CSS v3
- **상태 관리**: `Zustand` (도메인별 분리: `useMesStore`, `useSettingsStore`, `useAppStore` 등)
- **컴포넌트 구조**: 
  - `src/design-system/`: 범용 UI 컴포넌트 (공용 컴포넌트 최우선 규칙 적용)
  - `src/features/`: 도메인 비즈니스 로직 및 페이지
  - `src/shared/`: 타입, 스토어, 유틸리티

## 3. 다음 세션(Next Session) 진행 목표 🎯

다음 세션에서는 현재 미구현된 **템플릿 빌더**와 새롭게 정립된 **'모던 대시보드 구조'**를 다른 주요 화면에 이식하고, 기초 데이터를 연동해야 합니다.

1. **커스텀 견적서 양식 빌더 구현 (우선 순위 1)**
   - `SettingsPage > TemplateTab`의 '새 양식 만들기' 기능 설계.
   - 사용자가 직접 견적서 출력 레이아웃을 디자인(Drag & Drop Grid)할 수 있는 에디터 모달/화면 구축.
2. **견적 관리 페이지 전면 리디자인**
   - 현재 구형 UI 상태인 `EstimatesPage.tsx`를 환경 설정 페이지와 동일한 수준의 프리미엄 레이아웃으로 개편.
3. **거래처(Clients) 및 자재(Materials) 기초 데이터 화면 개발**
   - 기초 정보 관리 화면 구현 및 Supabase DB 연동.
   - 폼 입력 시 새로운 `BaseInput`, `PhoneInput` 등 공용 포맷팅 컴포넌트 필수 활용.
4. **견적 / 단가 산출 로직 연동**
   - `Settings`에서 저장한 단가, 마진, 할인율 정책 전역 상태 값을 불러와서 견적 산출 로직(`Scales.md` 참조)에 100% 반영.

## 4. 특이사항 (주의점)
- `DIRECTIVE.md`와 `AGENTS.md`의 디자인 원칙(무조건 다크 테마, 공용 컴포넌트 필수 사용)을 엄격하게 지키세요. 인라인 스타일을 지양하고 반드시 `design-system/`의 공용 컴포넌트를 조립하여 만드세요.
- 레이아웃을 구성할 때는 단순한 박스 나열이 아닌, 캔버스-표면-돌출부(`bg-base` -> `bg-surface` -> `bg-elevated`)의 **깊이감(Depth)**과 **여백(Padding/Grid)**을 고려한 고급스러운 설계를 최우선으로 하세요.
